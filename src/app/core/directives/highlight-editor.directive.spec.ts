import { HighlightEditorDirective } from './highlight-editor.directive';

describe('HighlightEditorDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightEditorDirective();
    expect(directive).toBeTruthy();
  });
});
