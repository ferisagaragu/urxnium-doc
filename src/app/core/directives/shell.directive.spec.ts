import { ShellDirective } from './shell.directive';

describe('ShellDirective', () => {
  it('should create an instance', () => {
    const directive = new ShellDirective();
    expect(directive).toBeTruthy();
  });
});
